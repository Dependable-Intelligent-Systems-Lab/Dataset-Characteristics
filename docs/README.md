# D-ACE Documentation

The rendered documentation is built from `docs/index.md` using
[Material for MkDocs](https://squidfunk.github.io/mkdocs-material/), matching
the style used by projects such as pyCLAD.

Build locally from the repository root:

```bash
python -m pip install -r docs/requirements.txt
mkdocs serve
```
